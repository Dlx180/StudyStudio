# UnitReading Prompt v1 Placeholder

This directory is reserved for the first versioned UnitReading prompt.

The MVP prompt must return schema-validated candidate ReadingUnits with page ranges,
titles, summaries, and generation metadata. Long prompts should live here rather than
inside UI components.
