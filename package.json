{
  "name": "knowtree",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/web",
    "packages/shared"
  ],
  "scripts": {
    "web:dev": "npm --workspace @knowtree/web run dev",
    "web:build": "npm --workspace @knowtree/web run build",
    "api:test": "cd apps/api && pytest"
  }
}
